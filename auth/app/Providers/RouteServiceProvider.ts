// import { Route } from 'najs'
// import { webRoutes } from '../../routes/web'

export class RouteServiceProvider {
  public register() {}

  public boot() {
    // Route.middleware('default').group(webRoutes)
  }
}
